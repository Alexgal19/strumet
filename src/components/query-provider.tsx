"use client";

import React from "react";

// This file is intentionally left blank after removing TanStack Query.
// It previously contained the QueryClientProvider.
export function QueryProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

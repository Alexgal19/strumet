
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // All paths are now private by default and require authentication.
    // The Admin SDK used in API routes will bypass these rules.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
